// do while run only time if statment not staisfy but while do not run if condition not satisfy

let num = 0
while(num <= 10) {
    console.log(`${num}`);
    num += 2
}

let array = ["fahad", "wasay", "azan"]
let arr = 0
while(arr < array.length) {
    console.log(`${array[arr]}`);
    arr++
}

let scr = 1
do {
    console.log(`${scr} scr`);
    scr ++
} while (scr <= 10)

let scrs = 11
do {
    console.log(`${scrs} scrs`);
    scrs ++
} while (scrs <= 10)